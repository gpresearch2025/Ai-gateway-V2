apiVersion: v1
kind: ConfigMap
metadata:
  name: ai-gateway-config
data:
  PORT: "3001"
  JOB_WORKER_INTERVAL_MS: "2000"
  JOB_MAX_ATTEMPTS: "3"
  JOB_RETRY_BASE_MS: "2000"
  JOB_RETRY_MAX_MS: "60000"
